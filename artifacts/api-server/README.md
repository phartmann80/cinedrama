# Deprecated — production API is `/backend`

This directory was a Replit workspace artifact (`@workspace/api-server` with `workspace:*` and pnpm catalog imports). The gitignored `pnpm-workspace.yaml` meant it could not be installed from a fresh clone.

The standalone, VPS-deployable API is:

```
/backend
```

Run it with `cd backend && npm install && npm run dev`.
